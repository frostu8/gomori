class LogMessage {
    // this.message = the LogMessage content
    // this.level   = the LogMessage level

    constructor(message, level) {
        this.message = message;
        this.level = level;
    }
}

module.exports = LogMessage;
